const Header = ({title}) => {
    return(
        <header className="w-full bg-slate-900 text-white pb-4 text-2xl text-center">
            <h1>{title}</h1>
        </header>
    )
}

export default Header;